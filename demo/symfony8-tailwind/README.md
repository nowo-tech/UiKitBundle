# Ui Kit Bundle — Symfony 8 demo (Tailwind)

Same kitchen sink as `symfony8`, with `nowo_ui_kit.css_framework: tailwind`.

```bash
cp .env.example .env   # PORT=8093
make up
# open http://localhost:8093/
```
