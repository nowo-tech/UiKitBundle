name: ui-kit-bundle

services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ui-kit-bundle-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
      - XDEBUG_MODE=coverage
    command: ["tail", "-f", "/dev/null"]
    networks:
      - ui-kit-bundle-network

volumes:
  composer-cache:
    driver: local

networks:
  ui-kit-bundle-network:
    driver: bridge
